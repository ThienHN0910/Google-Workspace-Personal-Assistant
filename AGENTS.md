# Agent Guidelines

## Agent skills

### Issue tracker

Issues and specs live in GitHub Issues (uses the `gh` CLI). See `docs/agents/issue-tracker.md`.

### Pull requests & merge workflow

Standard flow for issue creation, PR opening, secrets hygiene verification, and merging into `main`. See `docs/agents/pr-merge-workflow.md`.

### Triage labels

Canonical triage roles mapped to 5 default labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repository layout. See `docs/agents/domain.md`.
